#!/bin/sh

choice=$(echo "play/pause\njump\nartist\nalbum\ntrack\nload\nclear\ntoggle" | dmenu)

case $choice in
    play/pause)
        mpc toggle
        ;;
    jump)
        dm-mpd -j
        ;;
    artist)
        dm-mpd -a
        ;;
    album)
        dm-mpd -l
        ;;
    track)
        dm-mpd -t
        ;;
    load)
        dm-mpd -p
        ;;
    clear)
        mpc clear
        ;;
    toggle)
        option=$(echo "random\nconsume\nrepeat" | dmenu)
        case $option in
            random)
                mpc random
                ;;
            consume)
                mpc consume
                ;;
            repeat)
                mpc repeat
                ;;
        esac
        ;;
esac
